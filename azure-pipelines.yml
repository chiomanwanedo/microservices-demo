trigger:
  branches:
    include:
      - main

stages:
  - stage: Test
    jobs:
      - job: EchoJob
        pool:
          name: 'Default'
        steps:
          - checkout: self
          - script: echo "✅ Pipeline runs. Loop coming next."
            displayName: 'Test Echo'
